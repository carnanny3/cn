org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental-compilation cache computes relative paths between the
# pub cache (C:) and this project (D:); that's impossible across drive
# letters on Windows and crashes the build. Disable it rather than move
# either directory.
kotlin.incremental=false
kotlin.incremental.useClasspathSnapshot=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
